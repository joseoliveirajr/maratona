#include <bits/stdc++.h>
#define _ ios_base::sync_with_stdio(0);cin.tie(0);
using namespace std;
 
int main(){ _
 
	int t;
	cin >> t;
 
	long long int l, r;
	for (int i = 0; i < t; i++) {
		cin >> l >> r;
		cout << l << " " << (l * 2) << endl;
	}
	
	return 0;
}